# InspectorX — чек-лист запуска

> **Ревизия 27.07.2026.** Исходный документ написан 11.07.2026 под **трек 1** — запуск
> старого продукта (Lovable) как витрины-сборщика лидов «завтра вечером». Этого не
> случилось: вместо трека 1 за две недели построен и выведен в прод **трек 2** — этот
> репозиторий (v2 по TARGET_FORMAT), домен `inspectorx.uz` отдан ему.
> Поэтому чек-лист пересобран: у каждого пункта — статус по факту v2, а пункты,
> которые были специфичны для v1 (edge-функции Lovable, AI-чат, экран Evaluation),
> помечены `н/п` — в v2 такого кода нет.
>
> Легенда: `✅` сделано и проверено · `❌` не сделано · `◐` частично · `н/п` не применимо
> к v2 · `?` не проверяемо из кода (настройка панели / офлайн-процесс).
>
> Исходные решения 11.07 (оплата, модель пейволла) сохранены ниже как контекст.

---

## Что изменилось по сравнению с планом 11.07

| План 11.07 | Реальность 27.07 |
|---|---|
| Запустить v1 как витрину, v2 строить 2–4 недели | v1 не запускали; v2 в проде на `inspectorx.uz` |
| Пейволл — гейт на фронте, RLS «в первую неделю после запуска» | RLS-граница сделана сразу; фронт-гейт её лишь отображает |
| Email-подтверждение отключить (меньше трения) | Решение изменено: подтверждение включено, флоу сделан (PR #6) |
| Контент — 5–10 товаров руками | Построен конвейер импорта; 342 published требования, 142 через гейт |

**Главный вывод ревизии:** код и данные ушли далеко вперёд плана, а **юридический
минимум и операционка (блоки D и E) не сдвинулись с 11.07 вообще**. Именно они, а не
код, сейчас отделяют продукт от приёма денег.

---

## Блок A — Пейволл и подписка

- ✅ Флаг подписки `profiles.is_subscribed` + ручная активация. RLS запрещает пользователю
  включить подписку себе (`20260711120001_rls.sql:169`). В проде 1 подписчик (демо).
- ✅ Гейт на детальных блоках — `CRequirementCard` (`locked` → `CPaywallGate`), плюс
  `CLockedValue` в метриках товара. **Сильнее плана:** граница на сервере
  (`requirement_details` закрыты RLS), фронт только рисует замок.
- ◐ Страница тарифа `/pricing` готова. **Цена 490 000 сум стоит в `src/config.ts` с
  пометкой «не утверждена»** — открытый вопрос к дяде с 11.07, не закрыт.
- ◐ Таблица заявок `subscription_requests` + RLS (insert всем, чтение своих) — работает,
  форма отправляет (`src/data/real.ts:619`). **❌ Уведомление нам в Telegram НЕ сделано** —
  edge-функций в проекте нет вообще. Заявка падает в базу молча.
- ✅ «Спасибо, свяжемся» — inline-состояние формы (`i18n/ru.ts:249`), отдельный роут не нужен.

## Блок B — Чистка витрины

- ✅ Заглушек «скоро / будет добавлено» в UI не осталось — **кроме одной**: футер
  `«Оферта · Политика конфиденциальности — скоро»` (`i18n/ru.ts:43`), см. блок D.
- `н/п` Тексты про ЕАЭС/РК на экране Evaluation — экран из v1.
- ✅ Пустые вкладки процедур — вопрос закрыт данными: транзит/реэкспорт/реимпорт
  наполнены лупом, у всех 4 витринных товаров 0 пустых секций.
- `н/п` Раскрытие дерева категорий (рассинхрон `category_id`) — баг v1; в v2 каталог другой.
- ✅ Поиск: товар без контента → «раздел в работе» + заявка (`CSearch` → `content_requests`).
- ❌ Колокольчик: в кабинете есть, но `user_notifications` — **0 строк**. Пункт «наполнить
  или скрыть» так и открыт.
- ✅ Лендинг «Один маршрут» (`LandingB`) + SEO/GEO (PR #2).
- ◐ Бренд: опечатки `InspektorX` нет; используются `ИнспекторX` (26) и `InspectorX` (9).
  Правила «где кириллица, где латиница» не записано — мелочь, но лучше зафиксировать.

## Блок C — Безопасность

- `н/п` Удалить edge-функцию `create-test-user`, `LOVABLE_API_KEY`, rate limit на AI-чат
  и `analyze-document` — всё это v1. **В v2 edge-функций нет ни одной.**
- ✅ RLS включён на всех таблицах; платный контент закрыт на сервере.
- ✅ `user_questions.user_id` — `not null` + политики «только свои» (пункт 11.07 выполнен
  в схеме v2 с самого начала).
- ✅ Во фронте только publishable-ключ; service-ключ живёт в `.env.importer` (gitignored).
- ❌ **Тестовые данные в проде живы:** 3 юзера `ix.regtest*.20260712@gmail.com`
  (неподтверждённые), 2 тестовые заявки в `subscription_requests` (одна прямо помечена
  «ТЕСТ витрины (можно удалить)») и 1 тестовый `content_request` («Молок»).
- `?` Капча на регистрацию — настройка панели Supabase, из кода не видно.
- ~~Email-подтверждение отключить~~ — **решение отменено 12.07**: подтверждение включено,
  флоу и шаблоны сделаны (PR #6). Осталась косметика: русские шаблоны писем.

## Блок D — Юридический минимум ⚠️ НЕ СДВИНУЛСЯ С 11.07

- ❌ Публичная оферта.
- ❌ Политика конфиденциальности. (В футере вместо ссылок — «скоро».)
- ✅ Дисклеймер «информационный сервис, не юридическая консультация» — в футере
  (`i18n/ru.ts:45`). AI-чата в v2 нет, так что остальные места неактуальны.
- ❌ Страница контактов с реквизитами ИП — роута нет.
- `?` Реактивация ИП — офлайн-процесс, статус мне неизвестен. **Блокер приёма денег
  и договора с Click.**
- `?` Риск: закон УЗ о ПД требует хранения на серверах в УЗ, Supabase — не в УЗ.
  Зафиксирован как известный риск, ничего не изменилось.

## Блок E — Деплой и операционка

- ✅ Vercel-прод, домен `inspectorx.uz` (HTTP 200, редиректы с www и дефисных версий),
  SPA-rewrites.
- `?` Бэкапы Supabase — настройка панели, из кода не проверить.
- ❌ **Sentry — нет** (ни в зависимостях, ни в проде).
- ❌ **Аналитика — нет.** В HTML прода 0 совпадений по gtag/Plausible/Метрике.
  Продукт в проде две недели вслепую: неизвестно даже, заходит ли кто-нибудь.
- ◐ Кнопка поддержки: в футере `Telegram: @inspectorx_uz`. Живёт ли аккаунт — не проверял.
- ✅ OG-теги, favicon, title/description (PR #2).
- `?` Мобильный smoke-тест: скрипт `scripts/walkthrough.mjs` есть; по живому домену
  после переезда на `inspectorx.uz` не прогонялся.

## Блок F — Контент

- ✅/◐ В проде: **265 товаров** в каталоге, **11 услуг**, **345 требований** (342 published,
  142 через ИИ-конвейер), 284 детали. Цель лупа достигнута: 4 витринных товара — 0 пустых
  секций из 7, все 11 услуг — 0 из 6.
  Оговорка: полное покрытие у 4 товаров, остальные ~261 — пустые карточки каталога
  (для них и работает «раздел в работе» + заявка).
- ✅ Тексты лендинга и страницы тарифа.
- ❌ 2–3 уведомления для колокольчика — 0 строк (см. блок B).

---

## Что реально осталось до запуска (по приоритету)

1. **Юр. минимум (блок D)** — оферта, политика, страница контактов, реактивация ИП.
   Единственный настоящий блокер приёма денег. Всё остальное — оптимизация.
2. **Уведомление о новой заявке** — сейчас лид падает в базу молча. Самый дешёвый пункт
   с самой обидной ценой ошибки (обе заявки в базе — тестовые, но механизм тот же).
3. **Аналитика + Sentry** — две недели прод работает без единой метрики.
4. **Чистка тестовых данных** — 3 юзера `ix.regtest*`, 2 заявки, 1 content_request.
5. **Цена тарифа** — решение дяди; в коде стоит 490 000 сум как неутверждённая.
6. **Колокольчик** — наполнить реальными изменениями или скрыть.
7. **Smoke-тест на мобильном** по живому `inspectorx.uz`.

## Историческая часть (решения 11.07.2026, не редактируется)

**Стратегия двух треков.** Трек 1 — запустить текущий продукт как витрину и сборщик
заявок, потом заморозка (только контент). Трек 2 — чистый проект по TARGET_FORMAT,
2–4 недели, затем переключение домена. *По факту трек 1 пропущен, живёт трек 2.*

**Реальность по оплате.** Click требует юрлицо/ИП + договор + ключи — это дни.
На запуске: пейволл с ручной активацией, заявка → связались → админ ставит флаг.
Параллельно — реактивация ИП, потом Click SHOP API на готовую страницу тарифа.

**Модель пейволла.** Бесплатно — поиск, страница товара, заголовки требований, первый
уровень. По подписке — детали карточек, уведомления, вопросы, услуги.
*Реализовано строже плана: граница не на фронте, а в RLS.*

**Правило заморозки** (действует): в этот код после запуска добавляется только контент,
новые фичи — через TARGET_FORMAT.md.
