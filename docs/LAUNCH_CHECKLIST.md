# InspectorX — чек-лист запуска

> **Ревизия 27.07.2026.** Исходный документ написан 11.07.2026 под **трек 1** — запуск
> старого продукта (Lovable) как витрины-сборщика лидов «завтра вечером». Этого не
> случилось: вместо трека 1 за две недели построен и выведен в прод **трек 2** — этот
> репозиторий (v2 по TARGET_FORMAT), домен `inspectorx.uz` отдан ему.
> Поэтому чек-лист пересобран: у каждого пункта — статус по факту v2, а пункты,
> которые были специфичны для v1 (edge-функции Lovable, AI-чат, экран Evaluation),
> помечены `н/п` — в v2 такого кода нет.
>
> Легенда: `✅` сделано и проверено · `❌` не сделано · `◐` частично · `н/п` не применимо
> к v2 · `?` не проверяемо из кода (настройка панели / офлайн-процесс).
>
> Исходные решения 11.07 (оплата, модель пейволла) сохранены ниже как контекст.
>
> **Сверка с продом 29.07.2026:** уточнены статусы по тестовым данным (вычищены),
> колокольчику (лента на моках), футеру, поиску и цифрам контента; добавлен блокер
> «мониторинг изменений не построен».

---

## Что изменилось по сравнению с планом 11.07

| План 11.07 | Реальность на 29.07 |
|---|---|
| Запустить v1 как витрину, v2 строить 2–4 недели | v1 не запускали; v2 в проде на `inspectorx.uz` |
| Пейволл — гейт на фронте, RLS «в первую неделю после запуска» | RLS-граница сделана сразу; фронт-гейт её лишь отображает |
| Email-подтверждение отключить (меньше трения) | Решение изменено: подтверждение включено, флоу сделан (PR #6) |
| Контент — 5–10 товаров руками | Построен конвейер импорта; 342 published требования, из них 139 через гейт конвейера и 203 из переноса v1 |

**Главный вывод ревизии:** код и данные ушли далеко вперёд плана, а **юридический
минимум и операционка (блоки D и E) не сдвинулись с 11.07 вообще**. Именно они, а не
код, сейчас отделяют продукт от приёма денег.

*Дополнение 29.07:* блокеров приёма денег оказалось два. Второй — **мониторинг изменений
законодательства не построен**, при том что тариф и лендинг продают «ленту изменений» и
дайджест (см. блок B и пункт 2 списка «Что реально осталось»).

---

## Блок A — Пейволл и подписка

- ✅ Флаг подписки `profiles.is_subscribed` + ручная активация. RLS запрещает пользователю
  включить подписку себе (`20260711120001_rls.sql:169`). В проде 1 подписчик (демо).
- ✅ Гейт на детальных блоках — `CRequirementCard` (`locked` → `CPaywallGate`), плюс
  `CLockedValue` в метриках товара. **Сильнее плана:** граница на сервере
  (`requirement_details` закрыты RLS), фронт только рисует замок.
- ◐ Страница тарифа `/pricing` готова. **Цена 490 000 сум стоит в `src/config.ts` с
  пометкой «не утверждена»** — открытый вопрос к дяде с 11.07, не закрыт.
- ✅ Таблица заявок `subscription_requests` + RLS (insert всем, чтение своих); форма
  отправляет (`src/data/real.ts:619`). **Уведомление в Telegram сделано 27.07** —
  триггеры на трёх таблицах заявок шлют сообщение боту через `pg_net`
  (`20260727120000_lead_notifications.sql`). Проверено живой заявкой с сайта: сообщение
  пришло. Ограничение: ретраев нет, если Telegram недоступен — уведомление потеряно,
  заявка нет.
- ✅ «Спасибо, свяжемся» — inline-состояние формы (`i18n/ru.ts:249`), отдельный роут не нужен.

## Блок B — Чистка витрины

- ✅ Заглушек «скоро / будет добавлено» в UI не осталось ни одной. Строка `ru.footer.legal`
  («Оферта · Политика конфиденциальности — скоро», `i18n/ru.ts:43`) — **мёртвый код**, она не
  рендерится нигде: футер кокпита (`CLayout.tsx:239-253`) выводит только дисклеймер, e-mail и
  копирайт, футер лендинга (`LandingB.tsx:470-495`) к i18n не обращается вовсе. То есть в футере
  нет ни ссылок на оферту/политику, ни даже обещания «скоро» — см. блок D.
- `н/п` Тексты про ЕАЭС/РК на экране Evaluation — экран из v1.
- ✅ Пустые вкладки процедур — вопрос закрыт данными: транзит/реэкспорт/реимпорт
  наполнены лупом, у всех 4 витринных товаров 0 пустых секций.
- `н/п` Раскрытие дерева категорий (рассинхрон `category_id`) — баг v1; в v2 каталог другой.
- ✅ Поиск ничего не нашёл (запрос ≥2 символов, 0 результатов) → кнопка «Оставить заявку» →
  `content_requests`, kind `missing_product` (`CSearch.tsx:29` и `:185-196`, тексты
  `i18n/ru.ts:75-79`). Уточнение 29.07: товар **без требований** поиск как раз находит —
  `searchProductsReal` (`real.ts:187-207`) ищет по products/aliases и наличие контента не
  учитывает, так что заявка из поиска на такой товар не уходит.
- ✅ Метка «раздел в работе» (`ru.service.inProgress`, `i18n/ru.ts:134`) — это про услуги,
  а не про поиск: она вешается на этапы из захардкоженного списка `IN_PROGRESS_STAGES`
  (`src/data/cross-links.ts:70-79`) в `CRequirementList.tsx:92-94`.
- ❌ Колокольчик: лента кабинета живёт **на мок-фикстурах** — `fetchChangeFeed` фильтрует
  `changeFixtures` по id портфеля (`src/data/index.ts:349-360`, фикстуры
  `src/data/mock/fixtures.ts:885`), привязанным только к сигаретам и молоку. Таблицу
  `user_notifications` фронт не читает **ни разу** (в `src/` она встречается только в
  `lib/database.types.ts`), поэтому её наполнение в UI не проявится вообще — прежний диагноз
  «0 строк в `user_notifications`» указывал не на ту таблицу. Реальные источники пусты:
  `change_events` = 0, `requirement_change_impacts` = 0, `user_notifications` = 0. Решение:
  либо строить мониторинг изменений, либо скрыть мок-ленту у реальных юзеров.
- ❌ **Мониторинг изменений законодательства не построен** (обнаружено 29.07). Тариф продаёт
  «Ленту изменений по вашим товарам: было/стало и что делать» и «Уведомления: email-дайджест
  и Telegram» (`i18n/ru.ts:235-236`), лендинг обещает то же в настоящем времени
  (`LandingB.tsx:41`, `:411`). Читающая половина есть (таблицы, RLS, лента в кабинете),
  производящей и доставляющей нет: `change_events` = 0, кода, который их создаёт, в репозитории
  нет, каталога `supabase/functions` не существует (в `supabase/` только `config.toml` и
  `migrations`), рассылок нет. Тумблеры «email-дайджест» и «Telegram» в кабинете пишут
  только в localStorage через мок-стор. До запуска: либо снять обещание из текстов тарифа и
  лендинга, либо построить мониторинг. **Второй блокер приёма денег наравне с юр. минимумом.**
- ✅ Лендинг «Один маршрут» (`LandingB`) + SEO/GEO (PR #2).
- ◐ Бренд: опечатки `InspektorX` нет; используются `ИнспекторX` (26) и `InspectorX` (9).
  Правила «где кириллица, где латиница» не записано — мелочь, но лучше зафиксировать.

## Блок C — Безопасность

- `н/п` Удалить edge-функцию `create-test-user`, `LOVABLE_API_KEY`, rate limit на AI-чат
  и `analyze-document` — всё это v1. **В v2 edge-функций нет ни одной.**
- ✅ RLS включён на всех таблицах; платный контент закрыт на сервере.
- ✅ `user_questions.user_id` — `not null` + политики «только свои» (пункт 11.07 выполнен
  в схеме v2 с самого начала).
- ✅ Во фронте только publishable-ключ; service-ключ живёт в `.env.importer` (gitignored).
- ✅ **Тестовые данные вычищены** (проверено 29.07.2026): в `auth.users` ровно 1 пользователь —
  демо-подписчик (подтверждён 11.07), ни одного `ix.regtest*`; в `profiles` 1 строка с тем же id
  (`is_subscribed=true`); `subscription_requests` / `content_requests` / `user_questions` пусты
  (0 / 0 / 0) — удалена в том числе тестовая заявка от 27.07, которой проверяли уведомления.
  *(До 29.07 здесь стояло ❌: 3 юзера `ix.regtest*.20260712@gmail.com`, 2 тестовые заявки и
  1 `content_request` «Молок».)*
- `?` Капча на регистрацию — настройка панели Supabase, из кода не видно.
- ~~Email-подтверждение отключить~~ — **решение отменено 12.07**: подтверждение включено,
  флоу и шаблоны сделаны (PR #6). Осталась косметика: русские шаблоны писем.

## Блок D — Юридический минимум ⚠️ НЕ СДВИНУЛСЯ С 11.07

- ❌ Публичная оферта.
- ❌ Политика конфиденциальности. (В футере нет ни ссылок на оферту/политику, ни даже
  упоминания «скоро» — `CLayout.tsx:239-253`, `LandingB.tsx:470-495`; строка `i18n/ru.ts:43`
  не рендерится.)
- ✅ Дисклеймер «информационный сервис, не юридическая консультация» — в футере
  (`i18n/ru.ts:45`). AI-чата в v2 нет, так что остальные места неактуальны.
- ❌ Страница контактов с реквизитами ИП — роута нет.
- `?` Реактивация ИП — офлайн-процесс, статус мне неизвестен. **Блокер приёма денег
  и договора с Click.**
- `?` Риск: закон УЗ о ПД требует хранения на серверах в УЗ, Supabase — не в УЗ.
  Зафиксирован как известный риск, ничего не изменилось.

## Блок E — Деплой и операционка

- ✅ Vercel-прод, домен `inspectorx.uz` (HTTP 200, редиректы с www и дефисных версий),
  SPA-rewrites.
- `?` Бэкапы Supabase — настройка панели, из кода не проверить.
- ❌ **Sentry — нет** (ни в зависимостях, ни в проде).
- ❌ **Аналитика — нет.** В HTML прода 0 совпадений по gtag/Plausible/Метрике.
  Продукт в проде две недели вслепую: неизвестно даже, заходит ли кто-нибудь.
- ◐ Кнопка поддержки: Telegram-контакт есть **только в футере лендинга**
  (`LandingB.tsx:485`, захардкоженный `https://t.me/inspectorx_uz`). В футере кокпита
  (`CLayout.tsx:239-253`) его нет — там только `hello@inspectorx.uz`, то есть на
  `/catalog`, `/product`, `/pricing`, `/cabinet` поддержки не видно. Строка
  `ru.footer.telegram` (`i18n/ru.ts:42`) не используется нигде. Живёт ли аккаунт — не
  подтверждено. Нужно: проверить/завести аккаунт и продублировать контакт в футер кокпита.
- ✅ OG-теги, favicon, title/description (PR #2).
- `?` Мобильный smoke-тест: скрипт `scripts/walkthrough.mjs` есть; по живому домену
  после переезда на `inspectorx.uz` не прогонялся.

## Блок F — Контент

- ✅/◐ В проде: **265 товаров** в каталоге, **11 услуг**, **345 требований** (342 published:
  139 через ИИ-конвейер и 203 из переноса v1; ещё 3 карточки конвейера — в draft, итого
  `origin=ai_pipeline` — 142), 284 детали. Цель лупа достигнута: 4 витринных товара — 0 пустых
  секций из 7, все 11 услуг — 0 из 6.
  Оговорка: полное покрытие у 4 товаров, остальные ~261 — пустые карточки каталога.
  На такой карточке показывается «Оставьте заявку — наполним и сообщим», но **кнопки нет**
  (`CProductPage.tsx:191-199`; строки `ru.product.notifyWhenReady`/`notifyDone`,
  `i18n/ru.ts:106-107`, не используются) — заявку можно оставить только из пустого поиска.
- ✅ Тексты лендинга и страницы тарифа.
- ❌ Контент для колокольчика: источник изменений не построен (`change_events` = 0), лента
  кабинета работает на мок-фикстурах (см. блок B). Наполнять нечего, пока нет мониторинга.

---

## Что реально осталось до запуска (по приоритету)

1. **Юр. минимум (блок D)** — оферта, политика, страница контактов, реактивация ИП.
   Блокер приёма денег.
2. **Мониторинг изменений законодательства** — второй блокер того же класса: тариф и лендинг
   продают ленту изменений и дайджест, а механизма нет (`change_events` = 0, рассылок нет).
   Либо построить, либо снять обещание из текстов `i18n/ru.ts:235-236` и `LandingB.tsx:41,411`.
3. **Аналитика + Sentry** — две недели прод работает без единой метрики.
4. **Цена тарифа** — решение дяди; в коде стоит 490 000 сум как неутверждённая.
5. **Колокольчик** — следствие пункта 2: либо реальные изменения, либо скрыть мок-ленту
   у реальных юзеров.
6. **Smoke-тест на мобильном** по живому `inspectorx.uz`.

Закрыто: ~~уведомление о новой заявке~~ — сделано 27.07, проверено живой заявкой;
~~чистка тестовых данных~~ — сделана, проверено 29.07 (см. блок C).

## Историческая часть (решения 11.07.2026, не редактируется)

**Стратегия двух треков.** Трек 1 — запустить текущий продукт как витрину и сборщик
заявок, потом заморозка (только контент). Трек 2 — чистый проект по TARGET_FORMAT,
2–4 недели, затем переключение домена. *По факту трек 1 пропущен, живёт трек 2.*

**Реальность по оплате.** Click требует юрлицо/ИП + договор + ключи — это дни.
На запуске: пейволл с ручной активацией, заявка → связались → админ ставит флаг.
Параллельно — реактивация ИП, потом Click SHOP API на готовую страницу тарифа.

**Модель пейволла.** Бесплатно — поиск, страница товара, заголовки требований, первый
уровень. По подписке — детали карточек, уведомления, вопросы, услуги.
*Реализовано строже плана: граница не на фронте, а в RLS.*

**Правило заморозки** (действует): в этот код после запуска добавляется только контент,
новые фичи — через TARGET_FORMAT.md.
