# Утренний отчёт ночной сессии — 13.07.2026

> Ночь по handoff-research-loop §6. Все 4 шага пройдены. Решения ночи —
> `research-loop/DECISIONS.md`; baseline — `baseline-2026-07.md`;
> проход 2 — `pass2-findings-2026-07-13.md`. Промпт не менялся, Bridge не тронут,
> в прод мимо гейта не писалось, discovery-агенты не запускались.

## TL;DR

1. **«Потеря карточек шин» — не баг.** Все 40 «пропавших» карточек лежат в review-очереди
   `import_items` с причинами; гейт работает по дизайну. Bridge не участвовал.
2. **Eval построен, baseline снят** (14 отчётов: 4 товара + 10 услуг). Главные цифры ниже.
3. **Проход 2 сработал:** цемент (2523) и шины (4011) дословно подтверждены в прил. 4
   ПКМ-43 — обязательная **сертификация**. 4 карточки в очереди готовы к подтверждению
   в один клик.
4. **Главное узкое место измерено:** 33 из 40 карточек в review потому, что отчёт сам
   пометил их needs_review (UZ-приложения). Лечится не промптом, а проходом 2 + UZ-веткой
   гейта (вопрос №1 ниже).

## Метрики (baseline, детали в baseline-2026-07.md)

| Метрика | Товары (4 отчёта) | Услуги (10 отчётов) |
|---|---|---|
| schema_valid | 4/4 | 9/10 (security — без JSON-блока) |
| Требований на отчёт | 10–12 | 16–31 |
| section_coverage | 43–57% (transit/re-export/re-import = 0 у всех) | 100% (все 6 этапов) |
| needs_review_rate | 60–100% | 26–83% |
| Полные карточки (unit+how_to+docs+санкция ₮) | 0–45% | 0–56% |
| citation_pass_rate (гейт, живой прогон) | 3/43 (7%) | не гонялись (решение ночи) |
| cross_model_agreement | n/a — вторых прогонов нет (compass-файлы = копии) | n/a |
| verified-карточек/час | n/a — очередь ни разу не разбиралась | n/a |

## Review-очередь: что и почему (43 items)

- **needs_review_from_report = 33** — модель честно пометила сомнение (в основном UZ-only
  перечни lex.uz). Системная причина, промптом не лечится.
- **quote_mismatch = 4**, **lexuz_unreachable = 2**, **act_repealed = 1** — рабочие классы
  гейта. 2 merged + 1 loaded + 1 item в статусе loaded = прошли.
- После прохода 2: **4 items обогащены дословными строками ПКМ-43** (метка «pass2
  2026-07-13» в review_detail) — подтверждение за человеком.

## Пополнение бэклога промпта (§5 handoff — НЕ применено, копится)

1. Подтверждено цифрами: **чек-лист секций обязателен** (§5.1) — transit/re_export/
   re_import = 0 во всех 8 продуктовых прогонах; «нет норм» от «не искал» не отличить.
2. **Новый класс фейла: отчёт без JSON-блока** (service--security). Кандидат: в конце
   промпта самопроверка «Часть 2 присутствует и парсится».
3. **how_to без source_act_url** — цемент 0%, маркетплейс 39%: шаги есть, ссылок на
   порядок нет. Кандидат: source_act_url обязателен для каждого шага или «источник
   не найден» в серые зоны.
4. **Санкция без суммы** — 4–69% по отчётам. Кандидат: требовать fine_bru или явное
   «санкция не установлена» с обоснованием.

## Вопросы (блокируют следующий шаг)

1. **UZ-ветка гейта** (verifier.py: сверка UZ-цитаты с UZ-страницей + флаг «нет RU»).
   Разблокирует автопроход needs_review-карточек после прохода 2. Делать? (Моя
   рекомендация: да — это код, не промпт, и класс фейла измерен: 33/40.)
2. **Подтвердить 4 обогащённых items** (сертификация шин/цемента) в админке — первые
   lawyer_verified из цикла + первый замер verified-карточек/час.
3. **10 сервисных отчётов**: гнать через импортёр в прод (появятся на витрине) или
   сначала dry-run с разбором? (Рекомендация: dry-run → разбор → прод.)
4. **Второй прогон 1–2 товаров другой моделью** (вручную в ChatGPT/Gemini) — иначе
   cross_model_agreement измерить не на чем.
5. **Мёрж PR #7** — вся ночная работа копится в той же ветке.

## Санитария

- Тесты: 80 зелёных (74 + 6 eval). Коммиты ночи: `7c7f3ae` (eval), `e72e601` (pass 2).
- Секреты не коммичены (.env.importer в gitignore, JB-ключ — publishable фронтенда).
- JurisBase: read-only + 5 джоб smart_import в processing_jobs (протокол mistakes соблюдён).
