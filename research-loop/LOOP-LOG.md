# LOOP-LOG — итерационный цикл наполнения прода

> Формат лупа утверждён пользователем 13.07.2026 (4 решения, см. DECISIONS):
> UZ-ветка гейта — да; агентный ресёрч пустых секций из официальных источников — да;
> сервисные отчёты в прод через гейт — да; цель: ≥3 требования на секцию товара /
> этап услуги (либо явный статус «норм нет»). Каждая итерация: прогон → замер прода
> глазами (measure_prod.py) → починка процесса по классам фейлов → снова.
> В прод — только через гейт. Промпт разморожен (baseline есть), правки — через DECISIONS.

## Итерация 1 (ночь 13.07, ~03:00)

**Сделано:**
- UZ-ветка гейта: `legal_quote_uz` + `verify_url` в моделях, ветка в verifier
  (сверка UZ-цитаты по UZ-странице, penalty 0.95; дедуп-ключ остаётся каноническим).
- `requeue_review.py`: ре-верификация review-очереди с pass2-оверрайдами
  (`pass2-overrides.json`), прошло → штатная загрузка лоадером, resolution='fixed'.
- Requeue 4 карточек ПКМ-43: **2 loaded + 1 merged** (конфликт санкций у 4-й —
  правильно остался человеку). В проде появилась **первая карточка цемента**.
- Импорт 10 сервисных отчётов: 9 прошли конвейер (~181 требование в очередь),
  **beauty-salon: 1 loaded** — первая сервисная карточка через гейт.

**Найденные классы фейлов → починки (сразу в коде, тесты зелёные):**
| Класс | Где | Починка |
|---|---|---|
| `services_oked_code_check` — okved текстом («47.91 Розничная…») валит весь ран | marketplace | нормализация okved в модели (как _clean_hs) + ре-импорт ✓ |
| `unit_not_found`: «гл. 3», «глава 7 (ст. 34-46)» не парсились | law-firm и др. | паттерн `ch.N` в refs.py ✓ |
| `unit_not_found`: «Статья N» не находится на UZ-страницах | многие | `N-модда.` в find_paragraph ✓ |
| отчёт без JSON-блока | security | скип, перезаказ отчёта — вопрос человеку |

**Числа очереди после импорта услуг:** ~217 review-items
(needs_review_from_report ≈ 95, quote_mismatch ≈ 48, unit_not_found ≈ 24, прочее).

## Итерация 2 (запущена)

- requeue --llm по всей очереди (claude -p same_meaning для fuzzy 0.60–0.85) — фон.
- после — замер прода measure_prod.py, числа сюда.
