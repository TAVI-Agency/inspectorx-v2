# LOOP-LOG — итерационный цикл наполнения прода

> Формат лупа утверждён пользователем 13.07.2026 (4 решения, см. DECISIONS):
> UZ-ветка гейта — да; агентный ресёрч пустых секций из официальных источников — да;
> сервисные отчёты в прод через гейт — да; цель: ≥3 требования на секцию товара /
> этап услуги (либо явный статус «норм нет»). Каждая итерация: прогон → замер прода
> глазами (measure_prod.py) → починка процесса по классам фейлов → снова.
> В прод — только через гейт. Промпт разморожен (baseline есть), правки — через DECISIONS.

## Итерация 1 (ночь 13.07, ~03:00)

**Сделано:**
- UZ-ветка гейта: `legal_quote_uz` + `verify_url` в моделях, ветка в verifier
  (сверка UZ-цитаты по UZ-странице, penalty 0.95; дедуп-ключ остаётся каноническим).
- `requeue_review.py`: ре-верификация review-очереди с pass2-оверрайдами
  (`pass2-overrides.json`), прошло → штатная загрузка лоадером, resolution='fixed'.
- Requeue 4 карточек ПКМ-43: **2 loaded + 1 merged** (конфликт санкций у 4-й —
  правильно остался человеку). В проде появилась **первая карточка цемента**.
- Импорт 10 сервисных отчётов: 9 прошли конвейер (~181 требование в очередь),
  **beauty-salon: 1 loaded** — первая сервисная карточка через гейт.

**Найденные классы фейлов → починки (сразу в коде, тесты зелёные):**
| Класс | Где | Починка |
|---|---|---|
| `services_oked_code_check` — okved текстом («47.91 Розничная…») валит весь ран | marketplace | нормализация okved в модели (как _clean_hs) + ре-импорт ✓ |
| `unit_not_found`: «гл. 3», «глава 7 (ст. 34-46)» не парсились | law-firm и др. | паттерн `ch.N` в refs.py ✓ |
| `unit_not_found`: «Статья N» не находится на UZ-страницах | многие | `N-модда.` в find_paragraph ✓ |
| отчёт без JSON-блока | security | скип, перезаказ отчёта — вопрос человеку |

**Числа очереди после импорта услуг:** ~217 review-items
(needs_review_from_report ≈ 95, quote_mismatch ≈ 48, unit_not_found ≈ 24, прочее).

## Итерация 2 (~03:30): requeue --llm + словари

- requeue --llm №1: +1 loaded (LLM same_meaning, conf 0.67). Мало, потому что процесс
  стартовал со старым кодом (без гл./модда-фиксов) и needs_review-флаг режет до цитаты.
- Новые классы фейлов → починки: адресаты услуг («исполнитель услуги», «руководитель»,
  staff… 26+22+22+18 вхождений) → словарь _ADDRESSEES; `service scope: licensed` →
  безопасное сужение до ОКЭД услуги (слота в enum нет).
- requeue №2 (новый код): **+2 loaded** (экосертификация отходов, возврат/обмен).

## Итерация 3 (~04:00): авточистка needs_review + агентная вычитка пограничных

- **pass2_autoclear**: дословная сверка ≥0.85 без LLM → снято 3 флага; пограничных 54,
  прочих 75.
- requeue №3: **+6 loaded** (в т.ч. через новый паттерн `ch.N` и licensed-фикс).
- **pass2_borderline v1 — ошибка лупа**: 31/31 ложных NO — при fallback «вся страница»
  агенту передавались первые 6000 знаков, а фрагмент дальше. Фикс: окно вокруг
  partial_ratio_alignment. **v2: 5 verbatim-замен**, 26 честных NO (страница реально
  не подтверждает — нужен другой акт: следующий слой ресёрча).
- requeue №4: **+3 loaded** (нулевая пошлина э/мобиля по ПП-3818 прил.1!, техрегламент
  колёсных ТС app1/p.5, инфосистема маркетплейса). 2 отката в review с уточнённой
  причиной (unit_not_found у якобы пограничных) — тоже сигнал.
- Фикс measure_prod: специфичные счётчики затирали общебизнесовые (не суммировались).

## Замер прода после ночи (04:15, ai_pipeline: 59 → 73 published)

- Товары: цемент 1 (было 0), э/мобиль 4 (было 2), шины 2, ЛКМ 0. До цели ≥3/секцию далеко —
  нужен агентный ресёрч по секциям (одобрен), не только вычитка отчётов.
- Услуги: аптека 0 gaps (эталон), кафе 2, новые 9 услуг — по 2–3 gaps (premises/changes/
  closure < 3). Общебизнесовые карточки наследуются всеми услугами — счёт честный.
- Review-очередь: 205 items (needs_review ~92, quote_mismatch ~48, unit_not_found ~26,
  act_not_found/прочее ~39).

## Итерация 4 (~05:00, автономный луп, Opus 4.8): ВЭД-процедуры из Таможенного кодекса

Топ-провал замера: у ВСЕХ товаров transit/re_export/re_import/export = 0 (7/7 gaps).
Это ВЭД-режимы — регулируются Таможенным кодексом (lex.uz/docs/2876352), применяются
ко всем товарам. Агентный ресёрч (WebSearch → Кодекс → извлечение статей тем же
экстрактором, что и гейт):

- **Найден дефект гейта:** `find_paragraph` брал вхождение статьи из ОГЛАВЛЕНИЯ кодекса
  (тело ~67 зн.), а не настоящую статью (тело 800–2300 зн.). Все кодексы на lex.uz с
  оглавлением ломались молча. Починка: выбирать вхождение с самым длинным телом. +тест.
- **Сид `seed_ved_procedures.py`:** 12 требований (3 на секцию × export/re_export/
  re_import/transit) с scope=all_products, дословные цитаты статей 29–34, 58–60,
  117–120. Все прошли гейт **conf=1.0** (цитата = подстрока страницы) → **12 loaded**.
- **Фикс measure_prod:** scope `all_products` не считался (искалось `"all"`).

**Замер после итерации 4:** все 4 товара — export/transit/re_export/re_import = 3
(было 0), gaps 7/7 → **3/7**. Остаток по товарам: product/realization/import
(доменные секции из DR-отчётов). ai_pipeline published 73 → 85.

## Итерация 5 (~05:30, автолуп): общие требования жизненного цикла бизнеса

Топ-провал: у почти всех услуг premises/changes/closure < 3. Как и ВЭД у товаров, эти
этапы общие для любого бизнеса → scope=all_business закрывает их для всего каталога.
Агентный ресёрч (WebSearch → акты → извлечение статей экстрактором гейта):

- **Помещение:** санитарный ЗРУ-393 (docs/2732584) ст.16, пожарный ЗРУ-226
  (docs/1521663) ст.10 + ст.14.
- **Изменения:** ГК ч.1 (docs/111181) ст.43 (учред. документы), ст.44 (госрегистрация),
  ст.49 (реорганизация).
- **Прекращение:** ГК ст.53 (ликвидация), ст.54 (уведомить регорган), ст.56 (кредиторы).
- Сид `seed_service_lifecycle.py`: 9 требований scope=all_business → **9 loaded**, conf 1.0.
- **Урок extraction:** первые цитаты (ст.16/10) не прошли (fuzzy 0.73/0.59) — lex.uz
  вставляет UI-шум («Предложения по документу…») МЕЖДУ пунктами списка, а гейт сверяет
  с сырым текстом. Фикс: цитата = один непрерывный пункт списка, без пересечения границ.

**Замер после итерации 5:** ВСЕ 11 услуг — **gaps 0/6** (каждый этап ≥3). Цель по услугам
достигнута. ai_pipeline published 85 → 94. Остаток — только товары (product/realization/
import — доменные секции).

## Итерация 6 (13.07 ~14:30, оборвана; сверено 17.07)

Сессия упала на API ConnectionRefused после того, как фоновый сид завершился exit 0.
Сверка по проду 17.07 (measure_prod): **сид итерации 6 записан** — import у всех
4 товаров = 3–5 (было 0), ai_pipeline published 94 → 97. Потерь нет. Замер/лог/коммит
итерации выполнены постфактум этой записью. Остаток по товарам: product/realization
(gaps 2/7 у всех четырёх).

## Итерация 7 (17.07, после восстановления контекста и фолоу-апов фазы 1)

- Фолоу-апы фазы 1 закрыты ДО разбора очереди (правило DECISIONS 16.07):
  gate.score/matched_scope, языково-нейтральный порог autoclear, quote_uz в requeue.
- **pass2_autoclear** (новый порог по score): снято 14 флагов needs_review
  (дословные сверки conf 0.94–0.95, раньше резались штрафом ×0.95 и старым кодом).
- **requeue_review**: **+27 loaded, +14 merged**, очередь 205 → 164.
  Новые уточнённые причины: art.197-1 и art.18-20 не парсятся (диапазоны и
  дефисные статьи — класс unit_not_found), домен «химическая продукция / ЛКМ»
  без полки в domains.yaml (no_dictionary_slot).
- **pass2_borderline**: 10 verbatim-замен (agent-вычитка), 5 честных NO, 47 пропущено;
  requeue №2: **+10 loaded, +2 merged**. Попутно закрыт класс no_dictionary_slot:
  алиас домена «химическая продукция / лакокрасочные материалы» → ["32"] в domains.yaml.
- **Класс фейла p.N на ПКМ-страницах**: UI-виджет lex.uz «Получить ссылку из элемента
  документа» предваряет каждый пункт — лукбихенд [.;)] его не видел. Починка в
  find_paragraph + тест (111 зелёных).
- **Gap-ресёрч product/realization** (2 агента: WebSearch → lex.uz → дословные цитаты,
  проверка verbatim по живым страницам): ЗоЗПП (14643) ст.6/10/12/18, ЗРУ-819
  (6392314) ст.24/42, ПКМ-75 (243233) п.20. Сид seed_product_realization.py:
  **5 loaded + 2 merged**, все conf=0.95, scope=all_products. Отклонено ресёрчем:
  64493 (404, правильный id ЗоЗПП — 14643), «ЗРУ-813» (не существует, техрегулирование
  = ЗРУ-819), ЗРУ-706 — это «О транспорте», не «О торговле».

**Замер после итерации 7: ВСЕ 4 товара — gaps 0/7, ВСЕ 11 услуг — 0/6.
Цель лупа (≥3 требования на секцию/этап, решение 13.07) достигнута для всего каталога.**
ai_pipeline published 97 → 139 (+42 за итерацию); очередь review 205 → 152.

## Следующие итерации (бэклог лупа)

1. «Честные NO» пограничных: агентный поиск ПРАВИЛЬНОГО акта/страницы (не только той,
   что указана в отчёте) — главный резерв очереди.
2. Агентный ресёрч пустых секций товаров (transit/re-export/re-import: Таможенный
   кодекс; export) и этапов услуг (premises/changes/closure) — одобрен пользователем.
3. unit_not_found: составные unit («прил. 1 ЗРУ-701»), «ст. 461-467» на UZ-страницах.
4. quote_missing: карточки без цитат — кандидат на возврат в DR или агентную добычу.
5. Разбор review-очереди человеком в админке — un-blocker для verified/час.
