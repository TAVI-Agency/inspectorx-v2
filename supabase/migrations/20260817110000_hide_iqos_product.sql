-- Скрытие товара «Стики IQOS» с витрины (без удаления данных).
-- Оборот нагреваемого табака в Узбекистане запрещён (ЗРУ-1098), а контент
-- карточки товара тонкий — владелец решил снять её с витрины полностью,
-- сохранив исходные данные (требования, цитаты, историю) в базе для
-- возможного возврата в будущем. Фронт уже фильтрует продукты/услуги по
-- is_active=true (searchProductsReal и т. п.); эта миграция задействует
-- тот же флаг для конкретного товара, не трогая схему.
--
-- Примечание: имя файла сдвинуто на 20260817110000 (после последней
-- миграции 20260817100000_degraded_half_quota.sql), а не 20260810120000,
-- как в исходном тикете — тот таймстемп уже занят
-- 20260810120000_photo_review_queue.sql, и дублировать версию миграции
-- нельзя (коллизия PK в supabase_migrations.schema_migrations).

update public.products
set is_active = false
where id = 'e95fd48c-697c-1f2a-7967-8e8db36b9f91';
